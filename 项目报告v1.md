# 项目报告 v1
---

## 目录

- [一、项目简介](#一项目简介)
- [二、项目结构](#二项目结构)
- [三、分模块实现方式](#三分模块实现方式)
  - [3.1 RAG 知识库模块](#31-rag-知识库模块)
  - [3.2 Agent 与 SubAgent 模块](#32-agent-与-subagent-模块)
  - [3.3 Pipeline 编排与流式输出](#33-pipeline-编排与流式输出)
  - [3.4 记忆模块](#34-记忆模块)
  - [3.5 合规模块](#35-合规模块)
  - [3.6 API 与前端](#36-api-与前端)
  - [3.7 模型工厂](#37-模型工厂)
- [四、测试结果](#四测试结果)
  - [4.5 典型 Bad Case 范例](#45-典型-bad-case-范例)
- [五、测试结果分析](#五测试结果分析)
- [六、总结与未来优化方向](#六总结与未来优化方向)
- [附录](#附录)

---

## 一、项目简介

### 1.1 业务背景

保险条款 PDF 通常篇幅长、层级深（章 / 节 / 条 / 附表），且含有大量**表格**（保障计划表、费率表、给付比例表等）。用户提问类型多样：

| 类型 | 示例 |
|------|------|
| 基础事实 | 「守御一生的等待期是多久？」 |
| 条款解释 | 「补偿原则是什么意思？」 |
| 多条件判断 | 「第 20 天疾病住院赔不赔？」 |
| 风险提示 | 「犹豫期后退保有什么损失？」 |
| 模糊问题 | 「等待期多久？」（未指明哪份保单） |
| 诱导性问题 | 「等待期是 0 天对吧？」（错误前提） |

传统关键词搜索难以理解语义；纯 LLM 又容易幻觉。本项目采用 **RAG + ReAct Agent**：先检索条款片段，再基于检索结果组织回答，并通过合规层约束输出格式。

### 1.2 核心能力

| 能力 | 实现位置 | 说明 |
|------|----------|------|
| PDF 入库 | `document_loader.py` + `orchestrator.upload_policy_pdf` | 内存解析，不存源 PDF |
| 混合检索 | `hybrid_knowledge.py` | Dense + BM25 + RRF + Rerank |
| 主 Agent | `insurance_agent.py` + `insurance_react_agent.py` | ReAct；`retrieve_knowledge` + 可选 `delegate_subagents` |
| SubAgent | `subagent_tool.py` + `subagent_runner.py` | 主 Agent 工具触发；并行检索后 LLM 汇总短文 |
| 会话记忆 | `layered_session_memory.py` | 按 session 隔离；80% 软视图 / 93% 硬压缩 |
| 长期记忆 | `markdown_long_term_memory.py` + `ltm_merge.py` | Markdown 文件；Agent 工具读写 + 回合后 LLM 合并 |
| 合规 | `validator.py` | Hook 清洗正文；免责/来源提示由前端展示 |
| 流式对话 | `streaming.py` + `server.py` | SSE：thinking / answer delta / done |

### 1.3 技术栈

- **Agent 框架**：AgentScope（ReActAgent、Toolkit、LongTermMemoryBase）
- **对话模型**：DeepSeek（默认）或 DashScope 通义
- **嵌入**：DashScope `text-embedding-v4`（1024 维）
- **向量库**：Qdrant 本地持久化
- **长期记忆**：Markdown 文件（`data/memory_store/{user_id}.md`）
- **Web**：FastAPI + Uvicorn + 原生 HTML/JS 前端

---

## 二、项目结构

```
Loop_Insurance_Agent/
├── src/
│   ├── config.py                 # Settings dataclass，集中读取 .env
│   ├── main.py / web_main.py     # CLI / Web 入口
│   ├── api/server.py             # FastAPI、SSE、静态资源、单例 Pipeline
│   ├── pipeline/
│   │   ├── orchestrator.py       # 编排中枢：chat / chat_stream / 入库
│   │   ├── agent_router.py       # TurnConfig：复杂度、检索预算、追问检测
│   │   ├── subagent_runner.py    # SubAgent 并行 + synthesize 汇总
│   │   ├── streaming.py          # AgentScope Msg → SSE 事件
│   │   └── question_decomposer.py
│   ├── agent/                    # 主/Sub Agent 工厂、Prompt、委托工具
│   ├── rag/                      # 切分、混合检索、限长
│   ├── memory/                   # 分层会话记忆、Markdown LTM
│   ├── model/                    # LLM / Embedding 工厂
│   └── compliance/validator.py
├── frontend/                     # index.html + app.js + styles.css
├── scripts/                      # 入库脚本、评测与消融实验
├── test/                         # 40 题题库、3 份条款 PDF、实验结果
└── data/                         # vector_store、memory_store（运行时）
```

**端到端数据流**：

```
用户输入
  → 前端 SSE 消费
  → FastAPI /api/chat/stream
  → InsuranceAgentPipeline.chat_stream()
      → resolve_turn_config() 生成 TurnConfig
      → _build_enriched_input() 拼装 XML 上下文
      → InsuranceReActAgent ReAct 循环（工具：retrieve / delegate）
      → ComplianceValidator 清洗
      → 更新 LayeredSessionMemory + 可选 Markdown LTM 合并
  → SSE 推送 thinking / answer / done

PDF 上传
  → pypdf 抽文本
  → clause_chunking 章节切分 + child 256 字窗口
  → Qdrant 向量 + BM25 索引 + parent_chunks.json
```

---

## 三、实现方式

### 3.1 RAG 知识库模块

#### 3.1.1 PDF 解析与入库流程

当前入库链路（`document_loader.load_pdf_bytes`）：

1. 使用 **`pypdf.PdfReader`** 逐页调用 `extract_text()`，以 `\n\n` 拼接为全文；
2. 调用 **`chunk_text_by_clauses()`** 做结构化切分；
3. child 块写入 Qdrant + 内存 BM25；parent 映射写入 `parent_chunks.json`；文件名登记到 `policy_manifest.json`。

**现有局限（与后续优化相关）**：`pypdf` 的 `extract_text()` 对 PDF **表格**支持较弱——多列表格常被抽成「按阅读顺序拼接的纯文本行」，列对齐关系丢失。后续切分按 256 字滑动窗口再切 child 时，**长表格极易被拦腰截断**，导致检索到的片段缺少完整行列语义。这在评测中表现为：涉及「计划一/计划二对比表」「给付比例表」「费用比例表」的题目，Agent 有时只能看到半张表或错位文本（详见第六章 PDF 表格优化）。

#### 3.1.2 章节切分策略（clause_chunking.py）

切分不是简单固定长度，而是先识别保险条款的**层级标题**，再在 section 内部做 child 滑动窗口：

| 优先级 | 匹配模式 | 示例 |
|--------|----------|------|
| 100 | `第{中文/数字}条` | 第三条 |
| 90 | `第{中文/数字}章/节` | 第二章 |
| 80 | `\d+\.\d+(\.\d+)?` | 2.8、5.1.3 |
| 70 | 中文序号 + 顿号 | 一、 |

- 过滤目录行：连续 `....` / `……`、页码引导行（如 `2.8 .............. 12`）；
- 在全文上试各模式，取**优先级最高且 match ≥ 2** 者作为切分依据；
- 每个 section 生成一个 **parent**（整节全文）和若干 **child**（默认 256 字、overlap 64）；
- 无法识别结构时，整篇作为「全文」块，`use_parent_return=False`。

Parent 展开机制：检索命中 child 后，若 `use_parent_return=True`，tool 返回的展示文本替换为 parent 整节内容，避免 Agent 只看到一个 256 字碎片。

#### 3.1.3 混合检索（HybridKnowledge）

`retrieve()` 完整流程：

1. **双路召回**：Qdrant 语义 Top-N + BM25 Top-N（中文单字 + 双字 bigram 分词）；
2. **RRF 融合**：`score += 1/(60+rank+1)`，合并两路排名；
3. **Rerank**（`reranker.py`）：RRF 0.35 + 语义 0.35 + BM25 0.15 + 词面重叠 0.15，加权融合（非 cross-encoder）；
4. **Parent 去重**：同一 parent 只保留最高分一条；
5. **格式化输出**：`[来源: {doc_id} | {clause_label} | 相关度: {score}]\n{display_text}`。

#### 3.1.4 工具返回限长

| 环境变量 | 默认 | 含义 |
|----------|------|------|
| `RAG_TOOL_LIMIT` | 3 | 单次最多返回块数 |
| `RAG_MAX_CHUNK_DISPLAY_CHARS` | 2000 | 单块字符上限 |
| `RAG_MAX_TOOL_RESPONSE_CHARS` | 8000 | 工具响应总字符上限 |

超出截断并附加 `...(内容已截断)`，防止 ReAct 上下文被 tool_result 撑爆。

#### 3.1.5 retrieve_knowledge 工具注册

`InsuranceAgentFactory.create()` 将 `knowledge.retrieve_knowledge` 注册到 Toolkit，并在 `func_description` 与 System Prompt 中注入本轮 **`max_retrievals`**（如「最多调用 5 次」）。`doc_id` 参数对应 `<policy_library>` 中的 PDF 文件名，支持按保单过滤。

---

### 3.2 Agent 与 SubAgent 模块

#### 3.2.1 主 Agent 搭建

- 基类：AgentScope **`ReActAgent`**；
- 子类：**`InsuranceReActAgent`**，覆写迭代入口，在每次 ReAct 循环开始前调用 `LayeredSessionMemory.on_iteration_start()` 检查是否触发**硬压缩**；
- **工具集**：`retrieve_knowledge` + 可选 `delegate_subagents`（`SUBAGENT_ENABLED=true`）；
- **长期记忆**：`long_term_memory_mode="agent_control"`，Agent 按需调用 `retrieve_from_memory` / `record_to_memory`；`record()` 不 dump 会话全文；回合后 `MemoryManager.update_after_response()` + `ltm_merge` 在关键词命中时 LLM 合并写入 Markdown；
- **合规 Hook**：`post_reply` 注册，每轮回复后剥离误带的免责声明块。

#### 3.2.2 System Prompt 策略

主 Agent Prompt（`insurance_agent.py`）为**静态模板 + 每轮动态刷新**：

- 占位符：`{max_main_agent_retrievals}`、`{subagent_min_questions}` 等；
- 核心约束：条款问题必须检索；引用格式 `[保单filename | 第X条]`；≥2 个独立子问题才建议 `delegate_subagents`；禁止在正文写免责声明。

每轮 `_configure_main_agent_for_turn()` 同步更新 `_sys_prompt` 与各工具的 `func_description`，形成 **Prompt + 工具描述** 双重约束。

#### 3.2.3 SubAgent 工具化唤起机制

这是本项目多子问题并行能力的核心设计，具体实现如下：

**① 谁决定调用 SubAgent？**

SubAgent **不是** Pipeline 层的 if-else 自动路由。Pipeline 只生成 `TurnConfig`（复杂度、检索预算等），**不**自动派发 SubAgent。是否委托完全由**主 Agent 在 ReAct 循环中**根据用户 Query 自行决定：单一问题 → 多次 `retrieve_knowledge`；多个互不依赖的子问题 → 调用 `delegate_subagents`。

**② SubAgent 无递归委托权限**

`InsuranceAgentFactory.create_subagent()` 仅为 SubAgent 注册 `retrieve_knowledge`，** deliberately 不注册** `delegate_subagents`。从 Toolkit 层面杜绝「SubAgent 再调 SubAgent」的链式递归。

**③ 框架层硬编码与运行时校验**

| 控制点 | 实现位置 | 默认值 | 行为 |
|--------|----------|--------|------|
| 最少子问题数 | `subagent_tool.delegate_subagents()` | 2 | 不足则返回错误 ToolResponse，拒绝执行 |
| 最多子问题数 | 同上 | 5 | `cleaned[:max_subagents]` 截断 |
| 并行度上限 | `subagent_runner.run_subagents()` | 3 | `asyncio.Semaphore(3)` |
| 汇总长度上限 | `subagent_tool` | 3500 字 | `truncate_with_notice()` |
| 功能总开关 | 同上 | 开启 | `SUBAGENT_ENABLED=false` 时返回明确错误 |

**④ 并行执行与汇总**

```
主 Agent 调用 delegate_subagents(questions=[...])
  → run_subagents() [asyncio.gather + Semaphore]
      → 每个子问题：create_subagent() → 独立 ReAct（仅 retrieve_knowledge）
      → 输入 = policy_context + "子问题 N：..."
  → synthesize_subagent_brief() [单独 LLM，stream=False]
      → 压缩为带 [保单 | 条款] 引用的短文
  → 写入主 Agent tool_result
  → 主 Agent 据此组织用户可见的最终回答
```

SubAgent 回答在汇总前还会截断至 2500 字（`_format_sub_results_for_synthesis`），避免汇总 prompt 过长。

#### 3.2.4 TurnConfig 轮次路由

`agent_router.resolve_turn_config()` 每轮生成：

| 字段 | 说明 |
|------|------|
| `complexity` | low / medium / high（启发式 + 可选 LLM JSON 精修） |
| `max_retrievals` | 主 Agent 本轮 retrieve 上限：3 / 5 / 6 |
| `reason` | 配置理由（日志） |

#### 3.2.5 Enriched Input 拼装

主 Agent 实际看到的用户消息结构：

```xml
<policy_library>      <!-- 已入库保单 filename 列表 -->
<current_turn_only>   <!-- 只答本轮，勿重复历史 -->
<retrieval_budget>    <!-- 本轮 retrieve 次数上限 -->
【本轮用户问题】       <!-- 原始 user_input -->
```

长期记忆不再由 Pipeline 注入 `<user_profile>`，由主 Agent 在 ReAct 中按需调用长期记忆工具。

**重要细节**：`memory_manager.update_after_response()` 基于 **plain user_input** 与助手回答合并 LTM，而非 enriched 版本，避免 policy_library 等注入内容污染长期记忆。

---

### 3.3 Pipeline 编排与流式输出

#### 3.3.1 InsuranceAgentPipeline（orchestrator.py）

| 方法 | 职责 |
|------|------|
| `initialize()` | 创建 Embedding、Qdrant、HybridKnowledge、ComplianceValidator、主 Agent |
| `chat()` / `chat_stream()` | 绑定 session → TurnConfig → enriched_input → Agent → 合规 → 更新记忆 |
| `upload_policy_pdf()` | 内存解析 PDF → 切分 → 写向量库 |
| `clear_policy_library()` | 清向量库 + manifest + 可选清 LTM |
| `_bind_session()` | 将 `agent.memory` 替换为对应 `LayeredSessionMemory` |

#### 3.3.2 SSE 流式事件（streaming.py）

| type | 触发条件 | 关键字段 |
|------|----------|----------|
| `thinking` | thinking 块 / tool_use / tool_result | `delta`, `stream_id`, `last` |
| `answer` | assistant 正文（无 tool_use） | 增量 `delta` |
| `done` | 流结束 | `answer`, `thinking`, `compliance` |
| `error` | 异常 | `message` |

增量逻辑：`_stream_snapshots[stream_id]` 存全文快照，`delta = text[len(previous):]`。

**评测 TTFT 定义**：从发起 `chat_stream` 到收到**第一个 `answer` 事件**的时间。该时间包含：TurnConfig 解析、ReAct 多轮 tool 调用（含 RAG 与长期记忆工具）、SubAgent 并行等待——**不是**裸 LLM 的首 token 延迟。

---

### 3.4 记忆模块

#### 3.4.1 短期会话记忆（LayeredSessionMemory）

按 `session_id` 隔离，存于**进程内存**（后端重启即丢失）：

| 层级 | 触发阈值 | 行为 |
|------|----------|------|
| **软层** | token ≥ 窗口 × 80% | 不修改 canonical；API 前生成动态视图（早期摘要 + 工具原文 + 中期摘要 + 最近 N 条） |
| **硬层** | token ≥ max(93%×窗口, 窗口−13000) | ReAct 每轮迭代前触发；快照入 `archives`，LLM 四段式摘要后**替换**工作区 |

`InsuranceReActAgent` 禁用 AgentScope 默认字符阈值压缩，统一走上述分层策略。

#### 3.4.2 长期记忆（Markdown LTM）

- 文件：`data/memory_store/{user_id}.md`，按用户 ID 持久化一份 Markdown；
- **不存**问答全文或条款检索结果；
- **Agent 工具**：`retrieve_from_memory` / `record_to_memory`（`long_term_memory_mode="agent_control"`）；
- **自动写入**：`LONG_TERM_AUTO_PROFILE=true` 时，`ltm_merge.should_write_ltm()` 关键词命中 → `PROFILE_MODEL_NAME` LLM 语义合并更新 Markdown。

#### 3.4.3 SessionManager

`dict[session_id, LayeredSessionMemory]` 维护多 Tab 会话；`clear(session_id)` 用于评测消融的轮间隔离。

---

### 3.5 合规模块

- `ComplianceValidator.process()`：检测条款类关键词 → 生成 `ComplianceMeta`；
- `strip_display_footers()`：移除 Agent 误带的免责声明/来源提示；
- 前端：`GET /api/ui-config` 全局免责；`done.compliance.show_source_notice` 控制消息下来源提示。

---

### 3.6 API 与前端

**主要 API**：`/api/chat/stream`（SSE）、`/api/upload`、`/api/documents`、`DELETE /api/sessions/{id}`、`/api/health`（含 `server_boot_id`）。

**前端**（`app.js`）：多 Tab 会话、`localStorage` 持久化 session_id、SSE delta 节流渲染、后端重启检测、Markdown + DOMPurify 消毒。

---

### 3.7 模型工厂

`chat_model_factory.py` 按 `LLM_PROVIDER` 创建对话模型；不同场景使用不同 model_name：

| 场景 | 配置项 |
|------|--------|
| 主/Sub Agent | `MODEL_NAME` |
| 复杂度评估 | `DECOMPOSE_MODEL_NAME` |
| 上下文压缩 | `COMPRESSION_MODEL_NAME` |
| 画像/记忆合并 | `PROFILE_MODEL_NAME` |

Embedding 经 `embedding_factory.py` 独立创建，与对话提供商解耦。

---

## 四、测试结果

### 4.1 评测体系

| 项目 | 说明 |
|------|------|
| 题库 | `test/测试问题.txt`，Q01–Q40，六类题型 |
| 参考答案 | `test/测试答案.txt`，规则 Rubric 自动评分（0/1/2） |
| 条款 PDF | 如意鑫、守御一生（智臻版）、臻宝贝 2026 |
| 指标 | 得分、耗时、首 token 时间（TTFT）、得分点/扣分点 |
| 脚本 | `run_eval_test.py`、`run_eval_memory_ablation.py`、`postprocess_eval.py` |

### 4.2 实验一：基线三轮评测（eval_20260608_160054）

| 指标 | 数值 |
|------|------|
| 答题次数 | 137 |
| 总得分 | **243 / 274（88.7%）** |
| 完全正确 / 部分 / 错误 | 113 / 17 / 7 |
| 平均回答耗时 | 18.32s |
| 平均 TTFT | 10.89s（P50 ≈ 6.8s，P95 ≈ 38s） |

**分题型正确率**：风险提示 97.4% > 基础事实 94.4% > 条款解释 92.3% > 多条件判断 91.7% > 模糊问题 77.8% > **诱导性问题 70.6%**。

**分轮得分**（注意：三轮 session 不同，轮间 session 隔离，**不清空**）：

| 轮次 | 得分 | 正确率 |
|------|------|--------|
| 第 1 轮 | 74/94 | 78.7% |
| 第 2 轮 | 82/90 | 91.1% |
| 第 3 轮 | 87/90 | 96.7% |

### 4.3 实验二：记忆消融（ablation_20260608_165329）

固定题集 47 题（40 + 重复 7 题），三轮完全相同顺序：

| 组别 | 轮间策略 | 总得分 | 正确率 | 平均 TTFT |
|------|----------|--------|--------|-----------|
| A 隔离组 | 每轮 clear_session + 清空 LTM | 230/282 | 81.6% | 11.84s |
| B 累积组 | 复用 session，LTM 累积 | 233/282 | 82.6% | 11.44s |
| 差值 | — | +3 | +1.0% | −0.40s |

两组第 1 轮均为 83/94（88.3%），基线可比。

### 4.4 性能基线（419 次作答汇总）

| 指标 | P50 | 平均 | P95 |
|------|-----|------|-----|
| TTFT | ~7s | ~11s | ~40s |
| 总耗时 | — | ~18s | — |

简单基础事实题 TTFT 约 2–4s；复杂多检索题（Q17、Q29）可达 60–88s。

### 4.5 典型 Bad Case 范例

以下摘录来自 `eval_20260608_160054` 与 `ablation_20260608_165329_persistent` 的真实回答，说明**模糊题**与**诱导题**的主要失分模式。

---

#### Bad Case 1：Q31 模糊问题 — 未澄清产品，直接给出单一等待期

**题目**：「这份保险的等待期是多久？」（用户未指明如意鑫 / 守御 / 臻宝贝）

**期望行为**：先追问「哪份保单」，或分三份产品分别说明；不应默认某一份。

**实际回答（基线第 1 轮，得分 0/2）**：

> 根据 **《同方全球守御一生（智臻版）医疗保险》** 第 **2.4 条（等待期）** 的规定：
>
> **等待期为自本合同生效日起三十日内（含第三十日）。**
>
> 在等待期内确诊的疾病……保险公司均不承担给付相应保险金的责任。

**问题**：用户说的是「这份保险」，知识库中有三份保单；Agent **未经澄清就直接锁定守御一生**，对年金/寿险「无等待期概念」也未提及。Rubric 要求 `must_any` 含「哪份/分别/不同」等澄清表述。

**对比：同题较好回答（基线第 3 轮，得分 2/2）**：

> ……分别说明三份保单的情况：
>
> | 保单 | 是否有等待期 | 具体规定 |
> | ……仅**守御一生**设有 **30 天**等待期……
>
> 请问您指的是哪份保单？

---

#### Bad Case 2：Q31 模糊问题 — 长 session 下「错误固化」（消融累积组）

**题目**：同上 Q31，累积组同一 session 内第 6 次被问到。

**实际回答（得分 0/2）**：

> 「守御一生·智臻版」医疗保险的等待期如下：
>
> **等待期：30天** ……
>
> ### 两种特殊情况：等待期为 0 天
>
> | 情形 | 等待期 |
> | **意外伤害** | ✅ **0天** |
> | **续保** | ✅ **0天** |

**问题**：

1. 仍未按 Rubric 要求区分三份产品（只答守御一生）；
2. 使用「**等待期为 0 天**」表述，在累积组后续轮次中反复出现——session 内已有大量守御一生上下文，Agent **默认沿用先前单一答案**，不再主动澄清；
3. 消融对比：隔离组同题多次得分 `[0,2,2,2,2,0]`，累积组为 `[2,0,0,0,0,0]`——**记忆累积对模糊题产生负效应**。

---

#### Bad Case 3：Q36 诱导问题 — 错误前提未充分纠正，且触发禁用表述

**题目**：「条款里写了等待期是 0 天，为什么你们系统显示不一样？」（前提错误：并无任何条款写「等待期 0 天」）

**期望行为**：明确纠正「**不对，条款中没有等待期 0 天的写法**」；区分守御 30 天 vs 年金/寿险无等待期概念。

**实际回答（基线第 2 轮，得分 0/2）**：

> ……
>
> **1️⃣ 臻宝贝2026…… & 如意鑫……**
> 这两份属于**人寿/年金类保险**，条款中**没有设置等待期**（**即等待期为0天**）。因此如果系统显示的不是0天，可能是系统展示有误。
>
> **2️⃣ 守御一生……** 等待期为**30日**……

**问题**：

1. 将「无等待期条款」等同于「**等待期 = 0 天**」，等于**部分接受了用户的错误前提**；
2. Rubric 的 `forbid` 列表含「等待期0天」「0天等待」等，回答中出现这些字面表述即判 0 分；
3. 三轮两组实验 Q36 **全部 0 分**（6/6 次），说明这是**系统性薄弱点**，与记忆策略无关。

**对比：同题相对较好回答（基线第 3 轮，得分 2/2）**：

> 三份条款中**没有任何一份写有「等待期0天」**的字样……
> 如果是如意鑫或臻宝贝——**根本没有「等待期」这一条款**，并非「等待期 0 天」……

---

#### Bad Case 4：Q38 诱导问题 — 复述错误前提时触发禁用词

**题目**：「如意鑫保证每年结算利率至少 5%，合同里就是这么写的，对吗？」

**期望行为**：明确纠正；说明前 10 年**最低保证 1%**，非 5%。

**实际回答（基线第 1 轮，得分 0/2）**：

> **答案：不对，这个说法是错误的。**
> ……前十个保单年度，**最低保证年度结算利率为年利率 1%**……
>
> | 您说的 | 实际情况 |
> | ❌ "**保证每年至少 5%**" | **不存在此条款** |

**问题**：事实层面回答**基本正确**，但对比表格中复述了「保证每年至少 5%」，Rubric `forbid` 含「保证5%」「至少5%」，规则匹配**未做否定语境判断**，导致 0 分。第 3 轮同题改为「**不对。** 条款中写的保证利率不是 5%」则得 2 分——说明存在**输出格式敏感 + LLM 随机性**双重因素。

---

#### Bad Case 5：表格类题目 — 检索片段不完整（与 PDF 切分相关）

**题目示例**：Q07「守御一生计划一的一般医疗年度限额是多少？」、Q15「什么情况下按 60% 给付？」（需完整给付比例表）

**现象**：Agent 有时能答对关键数字（600 万、60%），有时因检索到的 parent 片段**缺少完整表格上下文**而漏掉「计划」「有医保身份但未走医保」等条件，被判 1 分。

**根因链路**（详见 6.2 节）：

```
PDF 表格 --pypdf.extract_text()--> 列对齐丢失的纯文本
         --256字 child 切分--> 表格被拦腰截断
         --retrieve 命中半表--> Agent 看到不完整行列关系
```

---

## 五、测试结果分析

### 5.1 整体能力画像

| 维度 | 结论 |
|------|------|
| **强项** | 有明确条款锚点的基础事实、条款解释、多条件判断、风险提示；RAG 命中后正确率 85%–97% |
| **弱项** | 模糊题（Q31）、诱导题（Q36/Q38）；长表格相关题偶发缺条件 |
| **评分噪声** | 14 次「缺少条款引用」扣分为误扣（回答含 `第 **7.2 条**` 等 Markdown 格式，regex 未匹配） |

### 5.2 基线实验第 1 轮低分澄清

第 1 轮 78.7% 低于后两轮，主因是 11 次「缺少条款引用」误扣。修正 `has_clause_ref` 兼容 Markdown 后，第 1 轮理论可提升约 10 个百分点。**不能**将第 2/3 轮上升解读为跨轮记忆学习——三轮 session_id 不同，session 对话记忆轮间隔离。

### 5.3 记忆消融结论

- 累积 vs 隔离：**+1%**，错误题数相同（各 9）→ 记忆层对条款 QA **影响处于噪声级**；
- 两组第 2 轮同步下跌 → 主因 **LLM 随机性**；
- Q31 累积组显著更差 → 长 session **可能固化错误单一答案**；
- 累积组第 3 轮 TTFT +26% → 长上下文带来首响延迟。

### 5.4 性能分析

- TTFT 平均 ~11s ≈ 检索/工具等待 + ~7s 首 token 后生成；
- 长尾与 ReAct 多轮 retrieve、SubAgent 并行强相关；
- 优化 TTFT 需先拆分各阶段耗时（当前未打点）。
- 前端页面人工多次测试发现，对于初次询问的模糊问题和诱导性问题Agent的回答速度相对自动评测实验较快，5次重复测试中未出现回答失败的情况，上述现象可能与过长上下文或同时输入过多问题有关。

---

## 六、总结与未来优化方向

### 6.1 项目总结

作为第一个实战项目，我完成了从 PDF 入库、混合 RAG、ReAct 主 Agent + 工具化 SubAgent、分层记忆到 Web 流式对话的**全链路搭建与量化评测**。40 题标准题库综合正确率约 **82%–89%**（视实验设置与评分规则而定）。

SubAgent 采用**主 Agent 工具化唤起 + SubAgent 无 delegate 权限 + min/max/concurrency 硬约束**的设计，在可控前提下支持多子问题并行。记忆层采用 session 隔离 + Markdown LTM（Agent 工具读写 + 回合后 LLM 合并），消融实验表明对当前任务增益有限，但需警惕长 session 对模糊题的负效应。

### 6.2 未来优化方向

| 优先级 | 方向 | 具体措施 |
|--------|------|----------|
| **P0** | 诱导/模糊题行为 | 路由层或 Prompt 强制「先问哪份保单」「明确纠正错误前提」；Q36/Q31 few-shot；Rubric 增加否定语境判断 |
| **P0** | **PDF 表格读取与切分优化** | 见下方专节 |
| **P0** | 评分与引用格式 | 修复 `has_clause_ref` regex，兼容 `第 **X.X 条**` |
| P1 | TTFT 长尾 | 简单题短路 RAG；限制无效 retrieve 轮次；Pipeline 分阶段打点 |
| P1 | 评测规范 | 固定题集 + 轮间 clear_session/LTM 隔离 |
| P2 | Session 持久化 | 当前进程内、重启丢失 |
| P3 | Rerank 升级 | 评估 cross-encoder 或 LLM rerank |

#### 6.2.1 PDF 表格读取方式优化（重点）

**问题现象**

在评测与人工抽检中发现：涉及**保障计划对比表、给付比例表、费用比例表、现金价值表**等题目，Agent 偶发漏读列/行、混淆计划一与计划二、或只答出数字却缺少适用条件。例如 Q15（60% 给付条件）、Q07（600 万限额需对应计划）、Q21（3% 费用比例需对应保单年度）等。

**根因分析**

当前入库链路存在三层表格损伤：

```
① 抽取层：pypdf.extract_text()
   - 多列表格 → 按阅读顺序拼成纯文本，列边界丢失
   - 单元格内换行 → 行序错乱

② 结构层：clause_chunking 按「第X条」切 section 后
   - section 内统一 256 字滑动窗口切 child
   - 长表格常被切成多个互不完整的 child 块
   - 检索命中其中一个 child 时，parent 展开虽能补全 section，
     但若整张表跨多个 section 或 section 内表格极长，仍可能不完整

③ 检索层：ToolResponseLimits 单块 2000 字、最多 3 块
   - 宽表即使 parent 展开也可能被截断
   - Agent 看到的 tool_result 缺少完整行列关系
```

**建议优化路径**

| 阶段 | 方案 | 说明 |
|------|------|------|
| 抽取 | 引入 **pdfplumber / camelot / tabula** 等表格专用抽取 | 保留行列结构，输出 Markdown 表或 TSV |
| 结构 | **表格感知切分**：检测表格块 → 整表作为不可拆分单元入 parent；表格外文本仍走 clause 切分 | 避免 256 字窗口拦腰截断 |
| 索引 | 表格块单独 metadata 标记 `content_type=table` | retrieve 时可加权或专用 query |
| 检索 | 对表格式 parent 提高 `max_chunk_chars` 或单独通道 | 保证计划对比类问题一次返回整表 |
| 验证 | 针对含表条款建立回归题（Q07/Q15/Q21 等） | 量化表格优化效果 |

此优化与 Agent/Prompt 层互补：**即使用 Prompt 要求「对照表格作答」，检索层若只给到半张表，模型也无法可靠推理**。

### 6.3 个人收获

- 理解了 ReAct 中 **工具描述 + System Prompt + 运行时校验** 的分层约束方式；
- 通过消融实验认识到：实验结论必须建立在**固定题集 + 对照组 + 明确隔离策略**之上，否则易误判「跨轮学习」；
- 建立了含 TTFT、得分点/扣分点的自动化评测流水线，为后续迭代提供量化基线；
- 识别出 **PDF 表格 → 切分 → 检索** 链路是当前 RAG 的主要瓶颈之一，不仅限于 Agent 行为调优。

---

## 附录

- 详细实验数据：[`test/实验总结`](test/实验总结)、[`test/results/`](test/results/)
- 架构文档：[`docs/architecture.md`](docs/architecture.md)
- **代码规模**（约，不含评测产物）：核心源码 ~8,600 行（`src/` ~4,270 + `scripts/` ~2,450 + `frontend/` ~1,880）
- 复现命令：

```bash
python scripts/run_eval_test.py --rounds 3
python scripts/run_eval_memory_ablation.py --rounds 3
```
